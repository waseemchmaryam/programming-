#include<iostream>
using namespace std;
void present(int arr[num]);
main()
{
	int i,n,c,num,arr;
	cout<<"Enter the no of element "<<endl;
	cin>>num;
	for(i=0;i<n;i++)
	{
		cout<<"Enter the no "<<endl;
		cin>>arr[num];
	}
	c=arr[]
	cout<<c;
}
void present(int arr[num])
{

	int found=0;
	cout<<"Check the no is present ? ";
	for(i=0;i<num;i++)
	{
		if(arr[i]==num)
		found=1;
		break;
	}
	if(found)
	cout<<"No is present ";
	else
	cout<<"not the no is present ";
}}
